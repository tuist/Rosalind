{
  "name": "@tuist/rosalind",
  "devDependencies": {
    "vitepress": "^1.3.2",
    "wrangler": "^4.0.0",
    "esbuild": "0.27.2",
    "cookie": ">=0.7.0"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name tuist-rosalind --branch main"
  }
}
